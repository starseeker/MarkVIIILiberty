# Initial source projection

The initial projection selected the flywheel with incorrect identifier case and
therefore omitted it. The crossmember projection and six native views were
correct. The final projection uses the actual ClutchDrum_flywheel ID explicitly.
The native geometry is unchanged. The original renderer, receipt and projection
are retained; the six hash-verified native views are reused.
