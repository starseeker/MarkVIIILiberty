# Retained failed controls

`trial01` contains the initial candidate's affected BReps, frozen inputs, report,
failed checks and images. Forward webs intruded into the backing/lining; four
copper tails struck the earlier estimated bridge. Trimming the webs to the actual
receiving cylinder and refining the coarse bridge envelope resolved those clashes.

`trial02` has the bounded bridge relief and trimmed webs. All504 interference pairs
passed, but one long fitting's curved surface retained a spurious 63.459697 mm² patch
across its third steel bore. The whole solid/void intersection was empty while the
cylinder face intersection was not. The retained probes document this inconsistency.
Using the default rotational seam for the fitting cutter removed that face patch;
the thin backing retains its independently qualified 90° seam. The cutter dimensions,
position and intended removed material do not change. Whole seat coverage now passes.

The first whole-lining check used cylindrical masks for the three recessed steel
heads. Those left 3.34 mm² of the true conical-pocket footprint unexplained on the
curved surface. Exact independently constructed conical witnesses cover the full
pockets. Remaining complete-face support and displaced negative controls pass.

`initial_plan_missing_parent.json` accidentally substituted the candidate filename
into the rear-parent dependency of the material stage. No geometry ran in that
stage. Correcting the declaration reused the three already-passed stages. Failed
invocation and subsequent receipts are preserved in the pipeline evidence.

The initial STEP exchange found no material differences, but the long fitting's
whole-face integrator reported about1.24e-12 against a1e-12 error request. Verified
disjoint partitions and an independent refinement meet the original error and
material-coverage requirements. Native and STEP solids stay unchanged. The failed
exports, measurements and focused native probe are retained in the final candidate
diagnostics.

The first case-section view hid the rivet behind the bridge; the final view cuts
through the rivet axis for visibility. Display cuts never alter physical geometry.
