The reusable StepSolidMatcher proposes pairs using the same centroid/volume score as the qualified exporter, caching each imported signature once and each native volume once per request. The complete immutable STEP-report replay selected exactly the same imported solid index for all342 comparisons. For330 installed solids this reduces imported centroid queries from54,615 to330, and volume queries by the same amount; cache/load took6.19s and matching2.15s in this run. These timings cover matching only, not the complete geometric validation. Full strict material, tolerance and adaptive-mass acceptance remains required. The published exchange result retains its original checker implementation; the helper is qualified for subsequent exporters.
