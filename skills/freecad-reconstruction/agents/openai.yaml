interface:
  display_name: "FreeCAD Reconstruction"
  short_description: "Build and verify source-led FreeCAD assemblies"
  default_prompt: "Use $freecad-reconstruction to build and verify the next CAD work packet."
